rules_version = '2';
service cloud.firestore {
  function isUserAuthenticated(userID) {
    return request.auth.uid == userID
  }

  match /databases/{database}/documents {
    match /users/{userID} {
      allow read, update: if isUserAuthenticated(userID);
    }
  }
}
